# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LimeApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LimeApp
pod "PromiseKit", "~> 6.8"
 pod 'SwiftyJSON', '~> 4.0'
  pod 'Kingfisher'
  pod 'Alamofire'
  target 'LimeAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LimeAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
